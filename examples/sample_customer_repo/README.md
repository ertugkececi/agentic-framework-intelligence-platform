# Sample Customer Repository

This intentionally small repository expresses a framework through repeated examples:
all concrete `*Service` classes extend `BaseService`, carry `@business_service`, and call `logger.info`.
